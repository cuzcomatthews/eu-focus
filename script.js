const fs=require('fs');let c=fs.readFileSync('src/app/(dashboard)/focus/page.tsx','utf8');let o=fs.readFileSync('old.txt','utf8');let n=fs.readFileSync('new.txt','utf8');c=c.replace(o,n);if(!c.includes('isSearchExpanded')){c=c.replace('const [trackSearch, setTrackSearch] = useState(\" "\);','const [trackSearch, setTrackSearch] = useState(\\);\n  const [isSearchExpanded, setIsSearchExpanded] = useState(false);');}if(!c.includes('Search,')){c=c.replace('import {','import {\n  Search,');}fs.writeFileSync('src/app/(dashboard)/focus/page.tsx',c);console.log('DONE'); 
